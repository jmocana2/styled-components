import React from 'react';
import { shallow } from 'enzyme';
import BodyArticle from '.';

describe('dummy test', () => {
  it('dummy test', () => {
    const wrapper = shallow(<BodyArticle />);
    expect(true).toBe(true);
  });
});

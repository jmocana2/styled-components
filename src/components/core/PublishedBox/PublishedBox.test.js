import React from 'react';
import { shallow } from 'enzyme';
import PublishedBox from '.';

describe('dummy test', () => {
  it('dummy test', () => {
    const wrapper = shallow(<PublishedBox />);
    expect(true).toBe(true);
  });
});

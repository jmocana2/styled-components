import React from 'react';
import { shallow } from 'enzyme';
import HeaderArticle from '.';

describe('HeaderArticle', () => {
  it('dummy test', () => {
    const wrapper = shallow(<HeaderArticle />);
    expect(true).toBe(true);
  });
});

import React from 'react';
import { shallow } from 'enzyme';
import SVG from '.';

describe('dummy test', () => {
  it('dummy test', () => {
    const wrapper = shallow(<SVG />);
    expect(true).toBe(true);
  });
});

import styled from 'styled-components';

const StyledTitle = styled.p`
    font-size: 1.2rem;
    font-weight: bold;
    line-height: 1.1;
    text-transform: uppercase;
    margin: 0;
    padding: 0.6rem 0 0;
    position: relative;
    z-index: 2;
`;

export { StyledTitle };

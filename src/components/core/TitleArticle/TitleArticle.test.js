import React from 'react';
import { shallow } from 'enzyme';
import TitleArticle from '.';

describe('dummy test', () => {
  it('dummy test', () => {
    const wrapper = shallow(<TitleArticle />);
    expect(true).toBe(true);
  });
});

import React from 'react';
import { shallow } from 'enzyme';
import FooterArticle from '.';

describe('FooterArticle', () => {
  it('dummy test', () => {
    const wrapper = shallow(<FooterArticle />);
    expect(true).toBe(true);
  });
});
